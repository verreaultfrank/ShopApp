# Copilot Instructions

## Project Guidelines
- Preference: avoid using hard-coded multiple ".." relative path segments in design-time configuration; prefer robust upward directory search for config files (project root or known folder).